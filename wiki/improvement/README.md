# Improvement tracker

Operational memory for the [`Prediction-Improvement-Loop.md`](../Prediction-Improvement-Loop.md).

| path | what belongs here |
| --- | --- |
| [`access-requests/`](access-requests/) | owner-facing asks for keys, paid APIs, labeled wallets |
| [`tickets/`](tickets/) | bounded `IMP-NNN` plans with frozen gates |
| [`outcomes/`](outcomes/) | promote / reject / hold / blocked-on-access after measurement |

JSON examples and schemas: `packages/contracts/improvement/`.

Runtime dumps may also be written to `artifacts/improvement/` (gitignored). Prefer a wiki copy when humans need to see the decision later.
