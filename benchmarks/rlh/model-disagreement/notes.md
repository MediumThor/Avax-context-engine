# model-disagreement

Process regression for ensemble disagreement language.

Expected process:

- `confidence_source` must be `model-disagreement`;
- the loop restates ensemble disagreement and wide uncertainty;
- no uncalibrated percent claims in bull/bear case prose.

This fixture labels uncertainty provenance, not forecast accuracy.