# Contracts

Versioned machine schemas. Humans read `wiki/Data-Contracts.md` and `wiki/Recursive-Learning-Contracts.md`.

TypeScript and Pydantic types should be generated from these files. Do not hand-maintain a second incompatible copy.

Recursive Learning Harness schemas live in `recursive/` (`loop_schema_version` 1).
