# Cursor agent briefs — RLH wave 1

Each file is a launch prompt for a Grok 4.6 subagent. Full contracts live in `wiki/tasks/`.

Wave roster: `wiki/Agent-Roster.md`.

Do not launch two agents against the same write scope. Wave 2 stays gated.
